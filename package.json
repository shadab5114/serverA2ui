{
  "name": "a2ui-genui-playground",
  "version": "1.0.0",
  "description": "GenUI playground: a Python backend (server/: FastAPI + LangGraph + AG-UI) generates A2UI v0.9 grounded in @shadab5114/pds-core, rendered by the React client in client/.",
  "type": "module",
  "scripts": {
    "agui": "cd server && uv run python run.py",
    "test": "cd server && uv run pytest",
    "docs:genui": "node scripts/build-genui-page.mjs",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down"
  },
  "dependencies": {
    "@shadab5114/pds-core": "^1.0.0-alpha.8"
  }
}
